--[[
Made by: TheNonStopProgrammer
Explanation: Simple script that shows the hosts that are online, more of a demo than a real program.
]]
os.loadAPI("coreNetwork")
coreNetwork.login()
local event, identifier, sender, target, message
os.startTimer(2)
repeat
event, identifier, sender, target, message = coreNetwork.pullEvent()
until event == "timer" or identifier == "hosts"
if identifier == "network" then
for key,value in pairs(message) do print(value["name"]) end
end
coreNetwork.logout()