--[[
Made by: TheNonStopProgrammer
Explanation: Display the messages send to a computer, demo and networking testing program
]]
--os.setComputerLabel("comp1")
os.loadAPI("coreNetwork")
term.clear()
term.setCursorPos(1,1)
coreNetwork.login()
while true do 
local event, modemSide, senderChannel, replyChannel, message, senderDistance = coreNetwork.pullEvent()
if event == "network_message" then
print(event)
print(modemSide)
end
end